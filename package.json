{
  "name": "~TODO~",
  "version": "0.0.0",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "jest": {
    "preset": "ts-jest"
  },
  "scripts": {
    "dev": "ts-node src",
    "dev:debug": "node -r ts-node/register src",
    "build": "tsc",
    "start": "node dist",
    "test": "jest",
    "coverage": "jest --coverage"
  },
  "devDependencies": {
    "@types/jest": "^28.1.1",
    "@types/node": "^17.0.42",
    "jest": "^28.1.1",
    "ts-jest": "^28.0.4",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.3"
  }
}
